# --- LibreOffice / editor lock & temp files ---
.~lock.*#
*.tmp
*~

# --- OS junk ---
.DS_Store
Thumbs.db
desktop.ini

# --- Secrets / credentials (NEVER commit) ---
.env
.env.*
*.pem
*.key
credentials.json
aws_credentials*

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# --- Node ---
node_modules/
.next/
dist/
build/
npm-debug.log*

# --- Logs (rotating runtime logs; regenerated, noisy) ---
logs/

# NOTE: private/ (devlog + design specs) and data/ (collected CSVs) are now
# intentionally committed to this PRIVATE repo, per team decision 2026-06-06.

# --- Personal context handoff (NEVER push — Swaroop's local-only notes) ---
private/SESSION_CONTEXT.md

# NOTE: Dataset.csv and the hackathon PDFs are intentionally committed.
