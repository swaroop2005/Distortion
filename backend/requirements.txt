# ThalNet backend + ML — Python deps
# Web / API
fastapi==0.111.0
uvicorn[standard]==0.30.1
mangum==0.17.0            # FastAPI -> AWS Lambda adapter
pydantic==2.7.4
python-dotenv==1.0.1

# AWS (DynamoDB, Bedrock, SES, Step Functions)
boto3==1.34.131

# Data + ML (Model A willingness, Model B churn)
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0
joblib==1.4.2

# Notebook / EDA (training notebook; optional locally)
jupyter==1.0.0
matplotlib==3.9.0
